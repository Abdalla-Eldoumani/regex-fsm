import { Walkthrough } from '@/types/walkthrough'

export const algorithmWalkthroughs: Walkthrough[] = [
  {
    id: 'alg-parsing',
    name: 'Regex Parsing',
    description: 'How a regex string becomes an AST',
    steps: [
      {
        id: 'parse-overview',
        targetSelector: 'regex-input',
        title: 'Regex Parsing Overview',
        description: 'Parsing converts a regex string like "(a|b)*abb" into an Abstract Syntax Tree (AST). This happens in two phases:\n\n1. Tokenization: Break the string into tokens\n2. Parsing: Build a tree from the token stream',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'parse-tokens',
        targetSelector: 'regex-input',
        title: 'Tokenization',
        description: 'The tokenizer reads each character and produces tokens:\n\n"(a|b)*abb" becomes:\n  LPAREN, SYMBOL(a), UNION, SYMBOL(b), RPAREN, STAR, SYMBOL(a), SYMBOL(b), SYMBOL(b), EOF\n\nThe + symbol is context-sensitive: after a symbol/paren it\'s positive closure, otherwise it\'s union.',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'parse-grammar',
        targetSelector: 'regex-input',
        title: 'Grammar & Precedence',
        description: 'The parser uses recursive descent with this grammar:\n\n  regex  -> union\n  union  -> concat ((+ | |) concat)*\n  concat -> repeat+\n  repeat -> atom (* | + | ?)*\n  atom   -> SYMBOL | EPSILON | ( regex )\n\nPrecedence (low to high): union < concat < repeat < grouping',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'parse-ast',
        targetSelector: 'regex-input',
        title: 'AST Output',
        description: 'The result is a tree of RegexNode types:\n\n(a|b)*abb produces:\n  Concat(\n    Star(Union(Symbol(a), Symbol(b))),\n    Concat(Symbol(a), Concat(Symbol(b), Symbol(b)))\n  )\n\nThis tree drives all subsequent algorithm steps.',
        position: 'bottom',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-thompson',
    name: "Thompson's Construction",
    description: 'How the AST becomes an NFA',
    steps: [
      {
        id: 'thompson-overview',
        targetSelector: 'nfa-view',
        title: "Thompson's Construction",
        description: "Thompson's construction converts a regex AST into an NFA recursively. Each AST node produces an NFA fragment with exactly one start state and one accept state.",
        position: 'bottom',
        category: 'algorithm',
        lectureRef: 'Definition 4.3',
      },
      {
        id: 'thompson-base',
        targetSelector: 'nfa-view',
        title: 'Base Cases',
        description: 'Empty string (λ):\n  (start) --λ--> (accept)\n\nSingle symbol (a):\n  (start) --a--> (accept)\n\nEach creates 2 states and 1 transition.',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'thompson-concat',
        targetSelector: 'nfa-view',
        title: 'Concatenation (RS)',
        description: 'Connect N(R).accept to N(S).start via a λ-transition:\n\n  N(R).start → ... → N(R).accept --λ--> N(S).start → ... → N(S).accept\n\nThe new NFA starts at N(R).start and accepts at N(S).accept.',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'thompson-union',
        targetSelector: 'nfa-view',
        title: 'Union (R|S)',
        description: 'Create new start and accept states with λ-branches:\n\n         λ → N(R) → λ\n        /              \\\n  (start)              (accept)\n        \\              /\n         λ → N(S) → λ\n\nFour new λ-transitions, two new states.',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'thompson-star',
        targetSelector: 'nfa-view',
        title: 'Kleene Star (R*)',
        description: 'Adds a loop and a skip path:\n\n       λ → N(R) → λ\n      /    ↑  ↓    \\\n  (start)  λ  λ   (accept)\n      \\____________/\n             λ\n\n4 λ-transitions: start→N(R), N(R).accept→N(R).start (loop), N(R).accept→accept, start→accept (skip).',
        position: 'bottom',
        category: 'algorithm',
        lectureRef: 'Theorem 4.1',
      },
      {
        id: 'thompson-properties',
        targetSelector: 'nfa-view',
        title: 'Thompson NFA Properties',
        description: 'Key properties of Thompson\'s construction:\n\n1. Exactly one start state (no incoming edges)\n2. Exactly one accept state (no outgoing edges)\n3. At most 2 outgoing edges per state\n4. Linear size: O(m) states for regex of length m\n\nThese properties make the NFA efficient to construct and simulate.',
        position: 'bottom',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-lambda-closure',
    name: 'λ-Closure Computation',
    description: 'Finding all states reachable via λ-transitions',
    steps: [
      {
        id: 'closure-what',
        targetSelector: 'nfa-view',
        title: 'What is λ-Closure?',
        description: 'The λ-closure of a set of states S is the set of all states reachable from S by following zero or more λ-transitions.\n\nThis is essential for:\n- Subset construction (NFA → DFA)\n- NFA simulation\n- Computing initial DFA states',
        position: 'bottom',
        category: 'algorithm',
        lectureRef: 'Definition 4.5',
      },
      {
        id: 'closure-algorithm',
        targetSelector: 'nfa-view',
        title: 'Algorithm',
        description: 'Stack-based approach:\n\n1. Initialize result = S, push all of S onto stack\n2. While stack not empty:\n   a. Pop state q\n   b. For each λ-transition q --λ--> r:\n      - If r not in result: add r, push r\n3. Return result\n\nThis handles cycles without infinite loops by checking membership before adding.',
        position: 'bottom',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-subset',
    name: 'Subset Construction',
    description: 'Converting NFA to DFA (powerset construction)',
    steps: [
      {
        id: 'subset-overview',
        targetSelector: 'dfa-view',
        title: 'Subset Construction',
        description: 'Converts an NFA to an equivalent DFA. Each DFA state represents a SET of NFA states. Also called the "powerset construction".\n\nWorst case: 2^n DFA states for n NFA states (exponential blowup).',
        position: 'top',
        category: 'algorithm',
        lectureRef: 'Theorem 4.2',
      },
      {
        id: 'subset-start',
        targetSelector: 'dfa-view',
        title: 'Step 1: Initial State',
        description: 'The DFA start state = λ-closure({NFA start state})\n\nExample: If NFA start is q0 and q0 --λ--> q1 --λ--> q2:\nDFA start state = {q0, q1, q2}',
        position: 'top',
        category: 'algorithm',
      },
      {
        id: 'subset-worklist',
        targetSelector: 'dfa-view',
        title: 'Step 2: Worklist Algorithm',
        description: 'For each unprocessed DFA state S and each symbol a:\n\n1. Compute move(S, a) = {t | s in S and s --a--> t}\n2. Compute T = λ-closure(move(S, a))\n3. If T is empty → transition goes to trap state ∅\n4. If T is new → add to worklist\n5. Add transition S --a--> T',
        position: 'top',
        category: 'algorithm',
      },
      {
        id: 'subset-accept',
        targetSelector: 'dfa-view',
        title: 'Step 3: Accept States',
        description: 'A DFA state S is accepting if S contains any NFA accept state.\n\nThe trap state ∅ is never accepting — it has self-loops for all symbols, representing permanent rejection.',
        position: 'top',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-minimize',
    name: 'DFA Minimization',
    description: "Moore's partition refinement algorithm",
    steps: [
      {
        id: 'min-overview',
        targetSelector: 'dfa-view',
        title: 'DFA Minimization',
        description: "Moore's algorithm finds the minimum-state DFA by merging equivalent states. Two states are equivalent if they behave identically on all possible inputs.",
        position: 'top',
        category: 'algorithm',
      },
      {
        id: 'min-partition',
        targetSelector: 'dfa-view',
        title: 'Initial Partition',
        description: 'Start with two groups:\n- P1: All accepting states\n- P2: All non-accepting states\n\nThese are definitely non-equivalent (one accepts λ, the other rejects it).',
        position: 'top',
        category: 'algorithm',
      },
      {
        id: 'min-refine',
        targetSelector: 'dfa-view',
        title: 'Partition Refinement',
        description: 'Repeat until no more splits:\n\nFor each group G and each symbol a:\n  Check if all states in G go to the same group on input a.\n  If not, split G into subgroups based on target group.\n\nWhen no group can be split further, the partition is final.',
        position: 'top',
        category: 'algorithm',
      },
      {
        id: 'min-merge',
        targetSelector: 'dfa-view',
        title: 'Build Minimized DFA',
        description: 'Each final group becomes one state in the minimized DFA:\n- Pick a representative from each group\n- Transitions follow the original DFA but map to group representatives\n- Rename states cleanly (q0, q1, ... or A, B, C, ...)',
        position: 'top',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-asu',
    name: 'ASU Direct Construction',
    description: 'Syntax tree method (Aho-Sethi-Ullman)',
    steps: [
      {
        id: 'asu-overview',
        targetSelector: 'construction-method',
        title: 'ASU Syntax Tree Method',
        description: 'The Aho-Sethi-Ullman method builds a DFA directly from the regex, skipping the NFA entirely.\n\nKey idea: Number each leaf (symbol) in the syntax tree, then compute which positions can follow which other positions.',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'asu-augment',
        targetSelector: 'construction-method',
        title: 'Step 1: Augment the Regex',
        description: 'Append an end-marker # to the regex: (r)#\n\nThis creates a unique final position. The DFA will accept when it reaches a state containing this position.\n\nExample: (a|b)*abb becomes ((a|b)*abb)#',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'asu-positions',
        targetSelector: 'construction-method',
        title: 'Step 2: Number Positions',
        description: 'Assign a unique position number to each leaf:\n\n((a|b)*abb)#\n  1 2  3 4 5  6\n\nPosition 1=a, 2=b, 3=a, 4=b, 5=b, 6=#',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'asu-nullable',
        targetSelector: 'construction-method',
        title: 'Step 3: Compute nullable, firstpos, lastpos',
        description: 'For each AST node, compute:\n\n- nullable(n): Can this subtree match the empty string?\n- firstpos(n): Which positions can match the first symbol?\n- lastpos(n): Which positions can match the last symbol?\n\nExample for (a|b)*: nullable=true, firstpos={1,2}, lastpos={1,2}',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'asu-followpos',
        targetSelector: 'construction-method',
        title: 'Step 4: Compute followpos',
        description: 'For each position i, followpos(i) = set of positions that can immediately follow position i.\n\nRules:\n- For concat(l, r): every position in lastpos(l) gets firstpos(r) added to its followpos\n- For star(c): every position in lastpos(c) gets firstpos(c) added to its followpos',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'asu-build-dfa',
        targetSelector: 'dfa-view',
        title: 'Step 5: Build DFA',
        description: 'DFA start state = firstpos(root)\n\nFor each state S (set of positions) and symbol a:\n  U = union of followpos(p) for all p in S where symbol(p) == a\n  Add transition S --a--> U\n\nAccept states = states containing the end-marker position (#)',
        position: 'top',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-brzozowski',
    name: "Brzozowski's Derivatives",
    description: 'Building a DFA via symbolic differentiation',
    steps: [
      {
        id: 'brz-overview',
        targetSelector: 'construction-method',
        title: "Brzozowski's Derivatives",
        description: "Brzozowski's method builds a DFA where each state IS a regex expression. The derivative of a regex R w.r.t. symbol a gives a new regex representing \"what R can match after consuming a\".",
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'brz-derivative',
        targetSelector: 'construction-method',
        title: 'The Derivative Operation',
        description: 'D_a(R) = the regex that matches string w if and only if R matches aw.\n\nExamples:\n  D_a(a) = λ           (consumed the a, empty string left)\n  D_b(a) = ∅           (b doesn\'t match a)\n  D_a(a|b) = λ         (a matches the a branch)\n  D_a(a*) = a*         (consume one a, still have a*)',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'brz-rules',
        targetSelector: 'construction-method',
        title: 'Derivative Rules',
        description: 'D_a(symbol(b)) = λ if a==b, else ∅\nD_a(concat(L,R)) = concat(D_a(L), R) | (if nullable(L) then D_a(R))\nD_a(union(L,R)) = union(D_a(L), D_a(R))\nD_a(star(R)) = concat(D_a(R), star(R))\n\nAfter each derivative, simplify to a canonical form for state comparison.',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'brz-simplify',
        targetSelector: 'construction-method',
        title: 'Simplification',
        description: 'Simplification is critical for termination:\n\n  concat(λ, R) → R\n  concat(∅, R) → ∅\n  union(∅, R) → R\n  union(R, R) → R\n  star(∅) → λ\n\nSort union operands for canonical comparison. Without simplification, the algorithm may not terminate!',
        position: 'left',
        category: 'algorithm',
      },
      {
        id: 'brz-build-dfa',
        targetSelector: 'dfa-view',
        title: 'Building the DFA',
        description: 'Start state = the original regex (normalized)\n\nFor each state (regex R) and symbol a:\n  Compute D_a(R), simplify it\n  If this expression is new → it\'s a new state\n  Add transition R --a--> D_a(R)\n\nAccept states = states where nullable(R) = true\n\nThe DFA is guaranteed to be finite because there are only finitely many distinct derivatives of any regex.',
        position: 'top',
        category: 'algorithm',
      },
    ],
  },
  {
    id: 'alg-simulation',
    name: 'String Simulation',
    description: 'How automata process input strings',
    steps: [
      {
        id: 'sim-dfa',
        targetSelector: 'simulation-section',
        title: 'DFA Simulation',
        description: 'DFA simulation is straightforward:\n\n1. Start at the initial state\n2. For each symbol in the input:\n   - Look up the transition δ(current_state, symbol)\n   - Move to the target state\n3. After processing all symbols:\n   - Accept if current state is in the accept set\n   - Reject otherwise\n\nTime complexity: O(n) for string of length n.',
        position: 'bottom',
        category: 'algorithm',
      },
      {
        id: 'sim-nfa',
        targetSelector: 'simulation-section',
        title: 'NFA Simulation',
        description: 'NFA simulation tracks SETS of possible states:\n\n1. Start with λ-closure({start_state})\n2. For each symbol in the input:\n   - Compute move(current_states, symbol)\n   - Take λ-closure of the result\n3. After processing all symbols:\n   - Accept if any current state is in the accept set\n   - Reject if no current state is accepting\n\nTime complexity: O(n * m) for n-length string and m NFA states.',
        position: 'bottom',
        category: 'algorithm',
      },
    ],
  },
]
